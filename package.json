{
  "name": "security-pr-copilot",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm --workspace apps/web run dev",
    "build:web": "npm --workspace apps/web run build",
    "lint:web": "npm --workspace apps/web run lint",
    "test:api": "cd services/api && pytest",
    "format:api": "cd services/api && ruff format app tests",
    "lint:api": "cd services/api && ruff check app tests"
  }
}

